# Security notes

- Never publish `config.json`, `credentials.json`, or `token.json`.
- Never paste a real TimeTree `_session_id` into issues, screenshots, or logs.
- Rotate/revoke credentials immediately if they are accidentally exposed.
- This project depends on an undocumented TimeTree web endpoint; review changes before running updates.
- Always use a dedicated Google Calendar.
